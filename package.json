{
  "name": "f1-2026-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "F1 2026 race weekend monitor with live OpenF1 data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check calendar.js && node --check sync-stability.js"
  },
  "engines": {
    "node": ">=20"
  }
}
